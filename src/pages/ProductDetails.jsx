import {useParams,Link} from 'react-router-dom'; import {products} from '../data/products'; import {ArrowLeft} from 'lucide-react';
export default function ProductDetails({onAdd}){const p=products.find(x=>x.id===Number(useParams().id)); if(!p)return <main className="page"><h1>Product not found</h1></main>; return <main className="page detail"><Link to="/products" className="back"><ArrowLeft size={16}/> Back to shop</Link><div className="detailGrid"><img src={p.image} alt={p.name}/><div><span className="tag">{p.category}</span><h1>{p.name}</h1><div className="bigPrice">₹{p.price}</div><p className="lead">A thoughtfully designed seed-paper product made with recycled paper and {p.seed} seeds.</p><div className="infoBox"><strong>🌱 Seed type</strong><span>{p.seed}</span><strong>♻️ Material</strong><span>Recycled seed paper</span></div><button className="primary" onClick={()=>onAdd(p)}>Add to cart</button><p className="plantNote">After use, tear into small pieces, place in soil, cover lightly, water gently and provide suitable growing conditions.</p></div></div></main>}
