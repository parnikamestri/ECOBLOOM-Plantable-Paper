import {createContext,useContext,useEffect,useState} from 'react';import {api} from '../api';
const C=createContext();export function AuthProvider({children}){const [user,setUser]=useState(null);const [loading,setLoading]=useState(true);useEffect(()=>{if(localStorage.getItem('ecobloom_token'))api('/auth/me').then(d=>setUser(d.user)).catch(()=>localStorage.removeItem('ecobloom_token')).finally(()=>setLoading(false));else setLoading(false)},[]);const login=async(email,password)=>{const d=await api('/auth/login',{method:'POST',body:JSON.stringify({email,password})});localStorage.setItem('ecobloom_token',d.token);setUser(d.user)};const register=async(name,email,password)=>{const d=await api('/auth/register',{method:'POST',body:JSON.stringify({name,email,password})});localStorage.setItem('ecobloom_token',d.token);setUser(d.user)};const logout=()=>{localStorage.removeItem('ecobloom_token');setUser(null)};return <C.Provider value={{user,loading,login,register,logout,setUser}}>{children}</C.Provider>};export const useAuth=()=>useContext(C);
